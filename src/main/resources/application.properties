# Application Configuration
spring.application.name=finance
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/finance_db
spring.datasource.username=finance_user
spring.datasource.password=finance_password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Session Configuration (for session-based auth)
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always

# Security Configuration
spring.security.user.name=kaiqueyamamoto
spring.security.user.password=B4aHwdyVX5RXal08eHzzGzydfAFHUAMhW7s61bktGU8=
spring.security.user.roles=USER,ADMIN

# Additional Admin User
spring.security.user.admin.name=admin
spring.security.user.admin.password=admin
spring.security.user.admin.roles=ADMIN

# Logging Configuration
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT Configuration
jwt.secret=FinanceAppSecretKey2024!@#$%^&*()
jwt.expiration=3600000

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always
management.endpoint.metrics.enabled=true
management.endpoint.prometheus.enabled=true
management.metrics.export.prometheus.enabled=true
management.metrics.export.prometheus.step=10s